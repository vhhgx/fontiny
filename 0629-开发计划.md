# Fontiny v0.2 SDK + CLI 开发计划

## Summary

将 Fontiny 从当前 Nuxt/Fontmin 半成品改造成 **可发布到 npm 的 Node.js 字体处理工具包**：核心是可链式调用的 SDK，CLI 只是 SDK 的命令行封装。第一阶段不做字重合并、不自研字体解析、不依赖 Fontmin、不强制 Python/fontTools，只使用 npm 可安装的 Node/WASM 生态包。

核心能力：

- `ttf/otf/woff/woff2` 输入字体子集化。
- 输出 `ttf/woff/woff2/svg`。
- SVG 图标目录生成 iconfont。
- 提供 Fontmin 风格 `.use(plugin())` 链式 API。
- 提供 `.text().formats().dest().run()` 便捷 API。
- 提供 CLI：`fontiny subset`、`fontiny convert`、`fontiny iconfont`。
- 为未来 Nuxt server/API 复用 SDK 做好接口设计。

## Key Changes

### 1. 包定位与依赖

将 npm 包定位为 **Node-only SDK + CLI**，浏览器端不直接使用。

推荐依赖：

```json
{
  "dependencies": {
    "commander": "latest",
    "fast-glob": "latest",
    "fonteditor-core": "latest",
    "fs-extra": "latest",
    "picocolors": "latest",
    "svgtofont": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "tsx": "latest",
    "tsup": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
```

删除或停止使用：

- `fontmin`
- npm 包里的 `child_process`
- 当前旧 CLI 依赖的 `utils/fos`、`utils/compression`
- Nuxt/WebSocket 作为本阶段主链路

保留当前 Nuxt 页面代码但不作为 npm 包核心发布内容；后续 Nuxt 版本只调用 SDK。

### 2. 包导出与目录结构

采用 TypeScript + ESM，构建产物输出到 `dist/`。

`package.json` 目标形态：

```json
{
  "name": "fontiny",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "fontiny": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./plugins": {
      "types": "./dist/plugins/index.d.ts",
      "import": "./dist/plugins/index.js",
      "require": "./dist/plugins/index.cjs"
    },
    "./iconfont": {
      "types": "./dist/iconfont/index.d.ts",
      "import": "./dist/iconfont/index.js",
      "require": "./dist/iconfont/index.cjs"
    }
  },
  "files": ["dist", "README.md", "LICENSE"]
}
```

建议源码结构：

```text
src/
  index.ts
  cli.ts
  core/
    Fontiny.ts
    asset.ts
    context.ts
    errors.ts
    scan.ts
    write.ts
    text.ts
  plugins/
    index.ts
    subset.ts
    convert.ts
    css.ts
    manifest.ts
  iconfont/
    index.ts
  commands/
    subset.ts
    convert.ts
    iconfont.ts
  config/
    loadConfig.ts
    schema.ts
```

当前 Nuxt 相关文件暂不删除；实现时避免继续从 `utils/extractify.js`、`utils/process.js` 扩展新逻辑。

### 3. SDK 链式 API

主入口：

```ts
import Fontiny from 'fontiny'
import { subset, convert, css, manifest } from 'fontiny/plugins'

await Fontiny()
  .src('input/**/*.{ttf,otf}')
  .use(subset({ text: '小楼一夜听春雨' }))
  .use(convert({ formats: ['ttf', 'woff', 'woff2', 'svg'] }))
  .use(css({ fontFamily: 'MyFont' }))
  .use(manifest())
  .dest('output')
  .run()
```

便捷 API：

```ts
await Fontiny()
  .src('input/**/*.{ttf,otf}')
  .text('小楼一夜听春雨')
  .formats(['woff2', 'woff'])
  .css({ fontFamily: 'MyFont' })
  .dest('output')
  .run()
```

便捷 API 内部等价于自动挂载插件：

- `.text(value)` 生成 `subset({ text: value })`
- `.formats(list)` 生成 `convert({ formats: list })`
- `.css(options)` 生成 `css(options)`

### 4. 核心类型与运行结果

核心资产类型：

```ts
type FontinyFormat = 'ttf' | 'otf' | 'woff' | 'woff2' | 'svg'

type FontinyAsset = {
  inputPath: string
  relativePath: string
  basename: string
  ext: FontinyFormat
  originalBuffer: Buffer
  currentBuffer: Buffer
  currentType: FontinyFormat
  outputs: Map<FontinyFormat | 'css' | 'json', Buffer | string>
  meta: {
    fontFamily?: string
    glyphCount?: number
    subsetText?: string
  }
}
```

插件接口：

```ts
type FontinyPlugin = {
  name: string
  transform(asset: FontinyAsset, ctx: FontinyContext): Promise<void> | void
}
```

运行结果：

```ts
type FontinyRunResult = {
  files: Array<{
    input: string
    outputs: string[]
  }>
  warnings: string[]
  errors: Array<{
    input?: string
    message: string
  }>
}
```

默认行为：

- 默认输出目录：`output`
- 默认格式：`['woff2']`
- 默认覆盖同名产物
- 默认保持输入目录相对结构
- 默认遇到单个字体失败时记录错误并继续处理其他文件
- 如果没有匹配文件，抛出明确错误

### 5. 字体子集化与转换

使用 `fonteditor-core` 作为默认引擎。

初始化：

- SDK 首次需要 WOFF2 时调用 `woff2.init()`
- 初始化只执行一次

`subset()` 插件能力：

```ts
subset({
  text?: string
  textFile?: string
  unicodes?: number[]
  hinting?: boolean
})
```

规则：

- `text`、`textFile`、`unicodes` 三者至少一个存在时才执行裁剪。
- `textFile` 内容读入后转换为 code points。
- 中文、英文、符号统一按 Unicode code point 处理。
- 输入 `otf` 时先读入并输出为内部 `ttf` buffer 再继续后续转换。
- 不做字重合并，不处理 variable font。

`convert()` 插件能力：

```ts
convert({
  formats: Array<'ttf' | 'woff' | 'woff2' | 'svg'>
})
```

规则：

- `ttf` 输出普通字体文件。
- `woff` / `woff2` 通过 `fonteditor-core` 写出。
- `svg` 输出 SVG font 文件，不代表单个 icon SVG。
- 输出文件名保留输入 basename，仅替换扩展名。
- 输出路径保留输入相对目录结构。

### 6. CSS 与 manifest

`css()` 插件生成 `@font-face`，默认每个字体生成一段 CSS，并可合并为 `fontiny.css`。

接口：

```ts
css({
  fontFamily?: string | ((asset: FontinyAsset) => string)
  basePath?: string
  local?: boolean
  filename?: string
})
```

默认：

- `fontFamily` 使用字体文件名。
- `basePath` 为相对路径。
- `filename` 为 `fontiny.css`。
- 优先引用 `woff2`，再引用 `woff`，再引用 `ttf`。

`manifest()` 插件生成 `fontiny.manifest.json`：

```json
{
  "files": [
    {
      "input": "input/cn/demo.ttf",
      "family": "demo",
      "outputs": ["output/cn/demo.woff2"]
    }
  ]
}
```

### 7. Iconfont API

Iconfont 单独提供入口，因为它是“一组 SVG 图标 -> 字体”，不是“字体文件 -> 字体”。

SDK：

```ts
import { iconfont } from 'fontiny/iconfont'

await iconfont()
  .src('icons')
  .name('fontiny-icons')
  .formats(['ttf', 'woff', 'woff2', 'svg'])
  .css(true)
  .types(true)
  .dest('output/icons')
  .run()
```

内部调用 `svgtofont`。

默认行为：

- 输入目录：`icons`
- 输出目录：`output/icons`
- 默认格式：`['ttf', 'woff', 'woff2', 'svg']`
- 默认生成 CSS
- 默认生成 `unicode` 映射 JSON
- 默认从 `E001` 开始分配 codepoint
- 支持固定 codepoint 映射文件，避免每次构建变化

### 8. CLI 设计

CLI 只调用 SDK，不直接写字体处理逻辑。

命令 1：字体子集化

```bash
fontiny subset "input/**/*.{ttf,otf}" \
  --text "小楼一夜听春雨" \
  --formats ttf,woff,woff2,svg \
  --out output \
  --css \
  --manifest
```

命令 2：字体格式转换

```bash
fontiny convert "input/**/*.{ttf,otf,woff,woff2}" \
  --formats woff2,woff \
  --out output
```

命令 3：SVG iconfont

```bash
fontiny iconfont icons \
  --name fontiny-icons \
  --formats ttf,woff,woff2,svg \
  --out output/icons \
  --css \
  --types
```

通用选项：

- `--config fontiny.config.js`
- `--out <dir>`
- `--formats <list>`
- `--verbose`
- `--silent`

配置文件示例：

```ts
export default {
  input: 'input/**/*.{ttf,otf}',
  output: 'output',
  text: '小楼一夜听春雨 深巷明朝卖杏花',
  formats: ['woff2'],
  css: true,
  manifest: true
}
```

优先级：

```text
CLI 参数 > 配置文件 > SDK 默认值
```

## Test Plan

### Unit tests

使用 `vitest`。

覆盖：

- `textToCodePoints()` 正确处理中文、英文、emoji、重复字符。
- glob 扫描能匹配 `ttf/otf/woff/woff2` 并保留相对路径。
- `.text().formats()` 能正确转换为插件流水线。
- 插件按 `.use()` 顺序执行。
- 没有匹配文件时报错。
- 单个文件处理失败时结果里包含错误信息。

### Integration tests

准备少量测试字体 fixture：

```text
fixtures/fonts/demo.ttf
fixtures/fonts/demo.otf
fixtures/icons/home.svg
fixtures/icons/user.svg
```

覆盖：

- `subset demo.ttf --text "abc"` 输出有效 `woff2`。
- `convert demo.ttf --formats ttf,woff,woff2,svg` 生成 4 类产物。
- `subset demo.otf --formats ttf,woff2` 能完成 OTF 输入转换。
- SDK 链式调用和 CLI 调用产物一致。
- `iconfont fixtures/icons` 输出字体、CSS、JSON。
- 生成的 CSS 引用路径正确。

### Manual acceptance

本地验证命令：

```bash
npm run build
node dist/cli.js subset "fixtures/fonts/*.{ttf,otf}" --text "你好Fontiny" --formats woff2 --out tmp/subset --css --manifest
node dist/cli.js convert "fixtures/fonts/*.ttf" --formats ttf,woff,woff2,svg --out tmp/convert
node dist/cli.js iconfont fixtures/icons --name test-icons --out tmp/icons --css --types
npm test
npm pack --dry-run
```

验收标准：

- npm 包内只包含 `dist`、`README.md`、`LICENSE` 等发布必要文件。
- 安装后能执行 `fontiny --help`。
- SDK 可被 Node ESM 项目 import。
- 不需要安装 Fontmin、Python、fontTools 或系统字体工具。

## Assumptions

- 第一阶段忽略字重合并、variable font、TTC/OTC。
- 第一阶段不重建 Nuxt UI，只保证 SDK 可在 Nuxt server/API 中调用。
- 默认字体处理引擎是 `fonteditor-core`；未来可追加可选 `fontTools` engine，但不是 v0.2 必需项。
- SVG 输出指 SVG font；SVG 图标转字体走独立 `iconfont()` API。
- 目标运行环境为 Node.js `>=18`。
- 发布包优先保持安装简单，不使用必须本机编译的 native addon。
