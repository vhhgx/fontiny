# Fontiny 后续功能规划

本文档记录 Fontiny v0.2 之后的功能方向。当前优先级是先增强“确认结果、真实项目可用性、排查问题”的能力，再继续扩展 UI 和高级字体能力。

## 1. inspect：字体信息查看

查看字体基本信息，帮助确认字体是否被正确读取、转换和输出。

示例：

```bash
fontiny inspect output/assets/fonts/ysbth.woff2
```

预期输出：

```text
file: output/assets/fonts/ysbth.woff2
format: woff2
family: ysbth
glyphs: 12
unicodes: 9
size: 1.0 KB
```

可展示内容：

- 文件格式
- 文件大小
- font family
- glyph 数量
- unicode 数量
- 支持的 unicode 列表
- name table 基础信息

## 2. check：文本覆盖校验

检查某个字体是否包含指定文本，比 inspect 更面向实际使用。

示例：

```bash
fontiny check output/font.woff2 --text "你好Fontiny"
```

预期输出：

```text
OK: all 9 characters included
```

如果缺字：

```text
Missing: 春 雨
```

适用场景：

- 子集化后确认指定文本没有丢字。
- 发布前校验字体是否覆盖页面所需字符。
- CI 中检查字体产物。

## 3. size report：体积报告

在 subset/convert 后自动输出体积变化，帮助确认子集化是否真正生效。

示例输出：

```text
ysbth.ttf
original: 1.3 MB
woff2: 1.0 KB
reduced: 99.9%
```

可支持：

- 命令行默认简洁报告。
- `--json` 输出结构化报告。
- manifest 中记录体积变化。

## 4. preview：预览页

预览页用于快速检查字体渲染、fallback 和缺字情况。

重要约定：preview 优先通过命令启动或打开，不要求每次 subset/convert 都生成新的 HTML 文件。

推荐命令：

```bash
fontiny preview output --text "你好Fontiny"
```

行为建议：

- 启动本地临时预览服务。
- 自动打开浏览器。
- 读取输出目录中的字体和 `fontiny.css`。
- 展示子集文本、对比文本、字体信息和文件大小。

页面内容：

- 当前字体列表。
- 指定文本渲染效果。
- fallback 对比。
- 缺字提示。
- 文件大小和格式。
- CSS 引用路径。

## 5. collect：从源码自动提取文本

扫描项目源码，提取需要保留的字符，生成文本文件供 subset 使用。

示例：

```bash
fontiny collect "src/**/*.{vue,ts,tsx,html,md}" --out chars.txt
fontiny subset input/font.ttf --text-file chars.txt --formats woff2 --out output
```

可支持：

- 中文字符提取。
- 英文、数字、标点提取。
- 自定义 include/exclude glob。
- 去重和排序。
- 忽略注释、代码标识符等高级策略后续再做。

## 6. CSS 增强

当前 CSS 只生成基础 `@font-face`。后续应增强为可直接用于真实前端项目。

可增加：

- `font-weight`
- `font-style`
- `font-display`
- `unicode-range`
- base64 内联
- 多字体合并 CSS

### 多字体合并 CSS 示例

输入目录：

```text
input/
  brand/
    Brand-Regular.ttf
    Brand-Bold.ttf
  title/
    Title-Regular.ttf
```

执行：

```bash
fontiny subset "input/**/*.ttf" \
  --text "你好Fontiny" \
  --formats woff2 \
  --out output \
  --css
```

输出：

```text
output/
  brand/
    Brand-Regular.woff2
    Brand-Bold.woff2
  title/
    Title-Regular.woff2
  fontiny.css
```

生成的 `fontiny.css`：

```css
@font-face {
  font-family: "Brand";
  src: url("./brand/Brand-Regular.woff2") format("woff2");
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Brand";
  src: url("./brand/Brand-Bold.woff2") format("woff2");
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Title";
  src: url("./title/Title-Regular.woff2") format("woff2");
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}
```

业务中只需引入一次：

```html
<link rel="stylesheet" href="/output/fontiny.css">
```

使用：

```css
.title {
  font-family: "Title", sans-serif;
}

.body {
  font-family: "Brand", sans-serif;
  font-weight: 400;
}

.strong {
  font-family: "Brand", sans-serif;
  font-weight: 700;
}
```

注意：多字体合并 CSS 不是字重合并。它保留多个字体文件，只是用一个 CSS 文件组织多个 `@font-face`。

## 7. init：配置初始化

快速创建项目所需配置和目录。

示例：

```bash
fontiny init
```

生成：

```text
fontiny.config.js
input/
icons/
output/
```

配置示例：

```ts
export default {
  input: 'input/**/*.{ttf,otf}',
  output: 'output',
  text: '小楼一夜听春雨 深巷明朝卖杏花',
  formats: ['woff2'],
  css: true,
  manifest: true
}
```

## 8. watch：监听模式

适合开发阶段持续生成字体产物。

示例：

```bash
fontiny subset "input/**/*.ttf" --text-file chars.txt --watch
```

监听对象：

- 输入字体文件
- 文本文件
- 配置文件
- SVG icon 目录

## 9. iconfont codepoints 固定

当前 iconfont 可以生成 unicode 映射，但后续需要支持固定映射，避免新增或删除图标后已有图标 unicode 变化。

示例：

```bash
fontiny iconfont icons \
  --name fontiny-icons \
  --codepoints iconfont.json \
  --out output/icons
```

`iconfont.json` 示例：

```json
{
  "home": "E001",
  "user": "E002",
  "setting": "E003"
}
```

## 10. rename / metadata 插件

修改字体内部 name table，适合品牌字体、私有字体和多字体规范化。

SDK 示例：

```ts
await Fontiny()
  .src('input/brand.ttf')
  .use(rename({
    family: 'BrandFont',
    subfamily: 'Regular',
    fullName: 'BrandFont Regular'
  }))
  .formats(['woff2'])
  .dest('output')
  .run()
```

可支持：

- family name
- subfamily name
- full name
- postscript name
- version

## 11. fonttools 可选引擎

默认仍然使用 Node/WASM 方案，保持 npm 安装简单。对于复杂 CJK、OpenType feature、可变字体等高保真场景，可后续增加可选 fontTools 引擎。

示例：

```bash
fontiny subset input.ttf --engine fonttools --text-file chars.txt
```

如果用户未安装 fontTools，则提示：

```text
fontTools engine is not available.
Install with: pip install fonttools brotli
Or use default engine: --engine builtin
```

## 12. Nuxt 集成

将当前旧 Nuxt UI 接入新的 SDK，而不是继续使用旧的 Fontmin/WebSocket 逻辑。

服务端示例：

```ts
import Fontiny from 'fontiny'

export default defineEventHandler(async () => {
  return await Fontiny()
    .src('/absolute/path/input/**/*.{ttf,otf}')
    .text('你好Fontiny')
    .formats(['woff2'])
    .dest('/absolute/path/output')
    .run()
})
```

集成目标：

- Nuxt server route 调用 SDK。
- 前端 UI 只负责配置、提交任务和展示结果。
- 复用 inspect/check/preview 能力。
- 移除旧 WebSocket 任意触发本机任务的安全风险。

## 建议实施顺序

推荐顺序：

```text
inspect -> check -> size report -> preview -> collect -> iconfont codepoints -> Nuxt UI
```

原因：

- `inspect`、`check`、`size report` 能直接解决“如何确认成功”的问题。
- `preview` 进一步提升人工验收效率。
- `collect` 让真实项目自动抽取字符。
- `iconfont codepoints` 提升图标字体长期维护稳定性。
- Nuxt UI 应建立在稳定 SDK 和验证能力之上。
